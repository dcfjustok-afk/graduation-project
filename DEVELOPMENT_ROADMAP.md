# 毕设后续开发总路线图

课题名称：基于区块链的可信任务日志审计系统  
当前状态：前端原型已完成第一版，已使用 React + TypeScript + Ant Design 搭建并可预览。

---

## 一、总原则

你接下来做这个毕设，建议遵循下面这套执行原则：

1. **你只负责少量必须人工完成的事情**
2. **绝大多数开发任务交给 AI 连续完成**
3. **按顺序推进，不要同时开太多模块**
4. **始终先做最小闭环，再做增强功能**
5. **每完成一个阶段，就立刻验证能不能运行**
6. **后端字段、复杂类型、最终业务细节可以晚一点完善**

---

## 二、你接下来要做的所有事情（按顺序）

下面是推荐的完整执行顺序。

---

# 第 0 步：确认当前基础状态

## 目标
确认当前仓库、前端页面、开发环境都处于可继续开发状态。

## 你要人工做的事情
- 确认本机 `Node.js` 和 `npm` 可以正常使用
- 确认当前前端页面能打开
- 确认 Git 仓库正常

## 这一步是否需要 AI
- **不需要专门开发**
- 这一步主要是人工确认

---

# 第 1 步：固定项目总体目录和开发顺序

## 目标
确保之后所有 AI 开发都严格围绕现有仓库结构进行，不乱建项目。

## 当前目录重点
- [apps/web](apps/web)：前端
- [apps/server](apps/server)：后端
- [apps/agent](apps/agent)：日志采集 Agent
- [packages/contracts](packages/contracts)：智能合约
- [packages/shared](packages/shared)：共享类型与公共逻辑
- [tests/performance](tests/performance)：性能测试
- [storage/sqlite](storage/sqlite)：数据库文件

## 你要人工做的事情
- 不需要额外做什么

## 需要 AI 做的事情
- 后续所有开发都必须基于这个结构继续

## AI 提示词
以后如果你担心 AI 跑偏，可以附加这段固定前置提示：

> 当前仓库已经确定为 monorepo 结构，请严格在现有目录下开发，不要重新创建新项目，不要修改整体目录规划。所有实现必须贴合当前毕业设计仓库结构继续推进。

---

# 第 2 步：继续稳定和润色前端原型

## 目标
把前端页面再提升一个层次，让它足够适合后续答辩展示，同时保持后端可扩展。

## 当前状态
这一步已经做了一部分，但还可以继续做：
- 更好的图表
- 更细的交互
- 更统一的视觉
- mock / 真实接口切换能力

## 你要人工做的事情
- 你只需要看页面效果，确认喜欢不喜欢
- 如果某些颜色、布局、风格不满意，再告诉 AI 继续改

## 需要 AI 做的事情
- 加图表
- 优化界面排版
- 提升卡片、状态、交互效果
- 支持 mock / 真实接口切换

## AI 提示词 1：继续优化前端展示

> 继续优化 [apps/web](apps/web) 的前端展示效果，在保持现有 React + TypeScript 架构不变的前提下，提高页面质感和展示效果。要求：1）不要改动整体页面结构方向；2）加强日志审计系统的科技感和管理系统风格；3）优化卡片、状态标签、间距、配色、图标层次；4）补充更适合毕设展示的图表区域；5）保持当前 mock 数据模式；6）直接修改代码并验证构建通过，不要问我。

## AI 提示词 2：支持 mock / API 可切换

> 把 [apps/web](apps/web) 当前前端结构改造成支持 mock 数据和真实接口可切换的模式。要求：1）保留现有页面和样式；2）抽离 API 层；3）通过配置控制当前使用 mock 还是请求后端；4）不要过早绑定复杂业务字段；5）方便后续后端接入时平滑替换；6）直接完成代码修改并验证通过。

---

# 第 3 步：初始化智能合约工程

## 目标
先把区块链部分搭起来，形成“日志上链存证”的基础能力。

## 为什么先做它
因为你的课题核心是“基于区块链的可信日志审计”，所以合约模块必须尽早落地。

## 你要人工做的事情
- 确认本机能安装 npm 依赖
- 后面如果涉及钱包私钥，自己保存

## 需要 AI 做的事情
- 初始化 Hardhat 工程
- 配置 TypeScript
- 安装 OpenZeppelin
- 创建合约、部署脚本、测试

## AI 提示词

> 在当前仓库下的 [packages/contracts](packages/contracts) 初始化 Hardhat 智能合约工程，使用 TypeScript，并接入 OpenZeppelin。要求：1）项目结构清晰；2）适合毕业设计讲解；3）创建最小可运行配置；4）提供基础部署脚本；5）提供基础测试；6）不要问我，直接安装依赖、创建配置文件和示例代码；7）完成后验证编译通过。

---

# 第 4 步：实现日志存证智能合约

## 目标
完成最核心的链上业务逻辑。

## 合约至少要支持
- 写入日志哈希
- 保存任务 ID
- 保存时间戳
- 保存提交者地址
- 支持权限控制
- 支持查询

## 你要人工做的事情
- 这一步基本不需要人工写代码

## 需要 AI 做的事情
- 编写 Solidity 合约
- 增加 `AccessControl`
- 编写合约测试
- 写注释，便于论文引用

## AI 提示词

> 基于 [packages/contracts](packages/contracts) 当前工程，为毕业设计实现日志存证智能合约。要求：1）合约支持写入日志哈希、任务ID、提交时间、提交者地址；2）使用 `AccessControl` 实现权限控制；3）支持按日志编号或任务ID查询；4）编写完整注释，方便写论文；5）编写完整测试；6）设计不要过度复杂，以稳定、清晰、适合答辩展示为主；7）直接完成代码并验证通过。

---

# 第 5 步：初始化后端工程

## 目标
搭建后端服务骨架，成为前端、Agent、区块链之间的中间层。

## 你要人工做的事情
- 这一步几乎不需要手动编码

## 需要 AI 做的事情
- 初始化 Node.js + Express + TypeScript
- 配置目录结构
- 创建基础入口
- 提供健康检查接口
- 配置统一响应格式

## AI 提示词

> 在当前仓库下的 [apps/server](apps/server) 初始化 Node.js + Express + TypeScript 后端工程，用于我的毕业设计“基于区块链的可信任务日志审计系统”。要求：1）使用 TypeScript；2）建立目录结构：`config`、`routes`、`controllers`、`services`、`repositories`、`db`、`blockchain`、`middleware`、`utils`；3）提供基础启动入口；4）提供健康检查接口；5）提供统一响应格式；6）保留后续区块链接入扩展位；7）直接创建代码、安装依赖并验证能运行，不要问我。

---

# 第 6 步：设计并实现 SQLite 数据库

## 目标
把链下存储定下来。

## 建议最少数据表
- `logs`
- `log_hash_records`
- `audit_records`
- `alerts`
- `agent_states`

## 你要人工做的事情
- 一般不需要手动写
- 只要最后看一下表名和结构是否容易理解即可

## 需要 AI 做的事情
- 设计表结构
- 落地建表脚本
- 组织初始化逻辑

## AI 提示词

> 在 [apps/server](apps/server) 中为日志审计系统设计 SQLite 表结构并直接实现数据库初始化。要求：1）至少包含 `logs`、`log_hash_records`、`audit_records`、`alerts`、`agent_states`；2）字段设计优先满足毕业设计展示；3）不要一开始做得过度复杂；4）每张表都要体现用途；5）要方便后续做审计查询和篡改实验；6）直接实现初始化代码并验证可用。

---

# 第 7 步：实现第一版后端 API

## 目标
先让后端具备服务前端和 Agent 的最小能力。

## 第一版 API 建议
- 健康检查接口
- 日志提交接口
- 日志列表接口
- 总览统计接口
- 告警列表接口
- 审计结果接口

## 你要人工做的事情
- 只需要后面看一下接口返回是否符合直觉

## 需要 AI 做的事情
- 编写路由、控制器、服务、仓储层
- 统一响应结构
- 接入 SQLite

## AI 提示词

> 在 [apps/server](apps/server) 中直接实现第一版后端 API，先服务于当前前端页面和后续 Agent 接入。要求：1）提供日志提交接口；2）提供日志列表接口；3）提供审计结果列表接口；4）提供告警列表接口；5）提供系统总览统计接口；6）当前先基于 SQLite；7）接口结构清晰，方便前端替换 mock；8）不要问我，直接落地代码并验证运行。

---

# 第 8 步：初始化 Agent 工程

## 目标
搭建日志采集 Agent，用来自动监听本地日志并上报。

## 你要人工做的事情
- 后面你要提供一个本地日志文件用于测试

## 需要 AI 做的事情
- 初始化 Node.js + TypeScript Agent
- 实现监听文件
- 实现增量读取
- 实现上报后端
- 实现失败重试和偏移量记录

## AI 提示词

> 在当前仓库下的 [apps/agent](apps/agent) 初始化 Node.js + TypeScript 的日志采集 Agent。要求：1）监听本地日志文件变化；2）支持增量读取新增内容；3）支持上报到后端；4）支持失败重试；5）支持偏移量保存；6）先做最小可运行版本；7）代码结构清晰，适合毕业设计说明；8）直接创建工程、安装依赖并验证运行。

---

# 第 9 步：打通 Agent 到后端最小闭环

## 目标
形成第一条真实业务链路。

## 闭环内容
日志文件新增内容  ->  Agent 监听  ->  后端接收  ->  SQLite 落库

## 你要人工做的事情
- 提供一个测试日志文件
- 运行一次，确认效果

## 需要 AI 做的事情
- 完成 Agent 与后端接口对接
- 提供演示数据
- 提供运行说明

## AI 提示词

> 把 [apps/agent](apps/agent) 和 [apps/server](apps/server) 打通，实现最小闭环：日志文件新增内容 -> Agent 监听 -> 调用后端接口 -> 后端保存日志记录。要求：1）先不强依赖区块链；2）接口保留扩展性；3）提供本地演示日志文件；4）提供运行说明；5）直接实现，不要只给方案。

---

# 第 10 步：后端接入区块链写入逻辑

## 目标
形成链上链下协同。

## 业务链路
Agent 提交日志  ->  后端计算哈希  ->  本地保存原文  ->  链上写入哈希与元数据

## 你要人工做的事情
- 提供本地测试钱包或本地链环境
- 保管私钥

## 需要 AI 做的事情
- 接入合约调用
- 记录交易哈希和区块号
- 处理异常情况

## AI 提示词

> 在 [apps/server](apps/server) 中接入 [packages/contracts](packages/contracts) 的合约调用逻辑。要求：1）后端收到日志后计算哈希；2）将原文保存到 SQLite；3）将哈希和元数据写入链上；4）把交易哈希、区块号、链上状态保存到数据库；5）保留失败重试和错误处理；6）当前先基于本地 Hardhat 网络；7）直接实现并验证最小闭环跑通。

---

# 第 11 步：把前端从纯 mock 改成可接真实后端

## 目标
让前端真正展示系统数据。

## 建议方式
保留 mock 模式，同时新增真实 API 模式，便于演示和调试。

## 你要人工做的事情
- 只需要确认页面显示的数据是否符合预期

## 需要 AI 做的事情
- 抽离 API 请求层
- 对接后端接口
- 保持页面稳定

## AI 提示词

> 把 [apps/web](apps/web) 从纯 mock 模式改造成“mock / 真实接口可切换”的结构。要求：1）保留现有页面样式和 TypeScript 架构；2）抽离 API 层；3）支持通过配置切换 mock 和真实后端；4）不要过早强绑定复杂字段；5）保证页面稳定；6）直接修改代码并验证构建通过。

---

# 第 12 步：实现审计逻辑

## 目标
真正完成“可信日志审计”。

## 审计逻辑至少包括
- 读取本地日志记录
- 重新计算哈希
- 比对链上哈希
- 判定是否异常
- 写入审计记录
- 生成告警

## 你要人工做的事情
- 几乎不需要自己编码

## 需要 AI 做的事情
- 在后端实现审计服务
- 在前端展示审计结果

## AI 提示词

> 在 [apps/server](apps/server) 中实现日志审计核心逻辑，并让 [apps/web](apps/web) 能展示审计结果。要求：1）支持重新计算本地日志哈希；2）支持与链上哈希比对；3）若不一致则生成异常记录和告警；4）把审计结果写入数据库；5）前端展示审计状态、异常结果和统计信息；6）直接实现完整代码并验证流程可运行。

---

# 第 13 步：实现告警与可视化增强

## 目标
让系统更适合展示和论文写作。

## 建议增强内容
- 日志趋势图
- 异常数量图
- 审计时间轴
- 告警卡片
- 状态分布图

## 你要人工做的事情
- 看最终页面是否适合作为答辩展示

## 需要 AI 做的事情
- 添加图表
- 做数据展示增强
- 做页面交互细节

## AI 提示词

> 在 [apps/web](apps/web) 中增强日志审计系统的可视化展示。要求：1）增加日志趋势图；2）增加异常分布图；3）增加状态统计图；4）增强审计时间线和告警展示；5）保持整体风格统一；6）兼容 mock 和真实接口模式；7）直接修改代码并验证构建通过。

---

# 第 14 步：补测试和实验脚本

## 目标
为论文实验章节准备材料。

## 至少要有
- 合约测试
- 后端接口测试
- Agent 基础测试
- 性能测试
- 审计测试

## 你要人工做的事情
- 跑一遍测试
- 保存实验结果

## 需要 AI 做的事情
- 写测试代码
- 写性能脚本
- 写实验脚本

## AI 提示词

> 为这个毕业设计项目补充测试与实验脚本，重点放在 [tests/performance](tests/performance) 和各模块自身测试中。要求：1）补充合约测试；2）补充后端接口测试；3）补充 Agent 基础测试；4）实现性能测试脚本，比如连续提交 100 条日志；5）实现批量审计测试脚本；6）输出适合论文记录的实验结果；7）直接落地代码，不要只写计划。

---

# 第 15 步：实现篡改实验

## 目标
做出论文里最关键的实验之一。

## 实验内容
- 正常写入日志
- 人为篡改本地日志或数据库内容
- 重新发起审计
- 展示异常检测结果

## 你要人工做的事情
- 人工确认实验截图与结果
- 保存论文要用的图和数据

## 需要 AI 做的事情
- 设计实验脚本
- 自动输出结果
- 提供演示步骤说明

## AI 提示词

> 为这个毕业设计实现“日志篡改检测实验”的完整演示方案。要求：1）构造正常日志写入流程；2）提供人为篡改 SQLite 或本地日志内容的方法；3）重新执行审计；4）输出篡改检测结果；5）给出实验脚本和演示说明；6）结果适合直接写入论文实验章节；7）直接实现代码和脚本。

---

# 第 16 步：整理论文可用技术文档

## 目标
让代码成果可以直接转成论文内容。

## 你要人工做的事情
- 最后自己检查措辞是否符合学校要求
- 把内容整理进论文模板

## 需要 AI 做的事情
- 根据真实代码整理技术说明
- 输出系统架构、模块、流程、数据库、测试等说明

## AI 提示词

> 基于当前仓库中的真实代码和目录结构，为我的毕业设计整理一份可直接用于论文写作的技术说明材料。要求：1）系统总体架构说明；2）模块划分说明；3）数据流说明；4）数据库设计说明；5）智能合约设计说明；6）审计流程说明；7）测试方案说明；8）内容必须贴合当前仓库实际代码，不要泛泛而谈。

---

# 第 17 步：整理答辩材料

## 目标
把系统成果转化为答辩展示内容。

## 你要人工做的事情
这一步是**必须人工参与**的：
- 挑选页面截图
- 选择实验结果图表
- 决定答辩演示顺序
- 组织 PPT 结构

## 可以让 AI 辅助做的事情
- 生成 PPT 文案提纲
- 生成演讲稿初稿
- 生成答辩问答清单

## AI 提示词

> 基于当前毕业设计项目，帮我整理一份答辩材料草案。要求：1）给出 PPT 章节结构；2）给出每页建议展示内容；3）给出系统演示顺序；4）给出常见答辩问题及参考回答；5）内容必须贴合“基于区块链的可信任务日志审计系统”这个课题。

---

## 三、哪些事情必须你本人手动做

下面这些事情不建议完全交给 AI：

### 1. 本机环境确认
- Node.js 是否正常
- npm 是否正常
- Git 是否正常
- 浏览器是否能打开页面

### 2. 钱包与密钥管理
- 测试钱包
- 私钥
- 助记词
- RPC 地址
- 合约部署后的地址记录

### 3. 学校材料相关
- 外文翻译
- 论文模板排版
- 学校要求的封面和格式
- 答辩提交材料

### 4. 最终展示确认
- 页面截图选取
- 实验结果筛选
- 论文插图确认
- 最终运行演示

---

## 四、哪些事情尽量全部交给 AI

这些部分非常适合交给 AI：

- 前端页面优化
- 前端图表与组件拆分
- 合约开发
- 合约测试
- 后端初始化
- SQLite 表结构实现
- REST API 开发
- Agent 监听与上报
- 区块链接入
- 审计逻辑实现
- 告警逻辑实现
- 性能测试脚本
- 篡改实验脚本
- 技术文档初稿
- 答辩提纲初稿

---

## 五、最推荐的执行顺序（精简版）

如果你只想看最核心顺序，就按下面来：

1. 继续微调前端展示
2. 初始化智能合约工程
3. 实现日志存证合约
4. 初始化后端工程
5. 设计 SQLite 并实现初始化
6. 实现第一版后端 API
7. 初始化 Agent 工程
8. 打通 Agent -> 后端 闭环
9. 打通 后端 -> 区块链 闭环
10. 前端接入真实后端
11. 实现审计逻辑
12. 增强可视化和告警
13. 补测试与实验脚本
14. 做篡改实验
15. 整理论文技术文档
16. 整理答辩材料

---

## 六、你现在最应该立刻开始的下一步

最建议你**下一条就让 AI 去做**：

### 优先级 1
初始化智能合约工程

### 优先级 2
初始化后端工程

### 优先级 3
把前端改成 mock / 真实接口可切换

这是目前最合理的起步顺序。

---

## 七、推荐你以后给 AI 下任务的固定格式

你以后每次发指令，尽量按这个格式：

1. 指明目录
2. 指明技术栈
3. 指明本阶段目标
4. 指明是否允许 mock
5. 指明不要问，直接做
6. 指明做完要验证

### 模板

> 在 [目标目录](目标目录) 中继续开发我的毕业设计模块。技术栈为 xxx。当前阶段目标是 xxx。可以先使用 mock 或最小实现，不要过早复杂化。不要问我，直接完成代码、依赖安装、配置和验证，并确保结果适合毕业设计展示与论文讲解。

---

## 八、最后建议

你现在最适合的推进方式是：

- **你负责做决策和验收**
- **AI 负责写代码、改代码、搭架构、补测试、写文档初稿**
- **每次只推进一个明确模块**
- **做完就验证，再进入下一步**

这样效率最高，也最适合你的毕设节奏。
