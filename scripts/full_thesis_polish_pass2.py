from __future__ import annotations

import shutil
from datetime import datetime
from pathlib import Path

from docx import Document
from docx.oxml import OxmlElement
from docx.table import Table
from docx.text.paragraph import Paragraph


DOCX_PATH = Path("output/Graduation-thesis.docx")
REPORT_PATH = Path("output/论文全文润色报告-pass2.md")


REPLACEMENTS = {
    "随着信息系统在业务处理、自动化运维和任务执行场景中的广泛应用，日志数据已成为安全审计、故障排查和责任追溯的重要依据。传统中心化日志通常将日志原文、哈希记录和审计结果保存在同一管理域内，存在被篡改、删除或伪造后难以及时验证的问题。针对上述问题，本文设计并实现了一种基于区块链的可信任务日志审计系统。系统采用“链下保存日志原文、链上保存日志哈希摘要”的混合存储模型，由 Agent 完成日志增量采集，Server 负责日志入库、SHA-256 哈希计算和 LogRegistry 智能合约调用，SQLite 保存日志、存证、审计和告警记录，Web 前端展示日志、审计和告警结果。审计阶段，系统重新计算日志原文得到 actualHash，并与数据库中的 expectedHash、链上的 onChainHash 进行三方比对；当哈希不一致时，生成 failed 审计记录和 hash_mismatch 告警。系统还针对本地链重启和多次部署场景，引入合约代码存在性校验与历史合约地址回溯策略，以减少空地址和历史记录误判。实验结果表明，LogRegistry 合约测试 8 项全部通过；日志批量提交 100 次成功率为 100%，平均响应时间约 107.03 ms，吞吐量约 9.33 条/秒；批量审计 100、500、1000 条数据 5 轮均执行成功；篡改检测实验能够识别日志内容被修改并生成告警。":
        "随着信息系统在业务处理、自动化运维和任务执行场景中的应用增多，日志数据已成为安全审计、故障排查和责任追溯的重要依据。传统中心化日志通常将日志原文、哈希记录和审计结果保存在同一管理域内，日志一旦被篡改、删除或伪造，后续校验缺少独立可信证据。针对该问题，本文设计并实现基于区块链的可信任务日志审计系统。系统采用链下保存日志原文、链上保存日志哈希摘要的混合存储模型，由 Agent 完成日志增量采集，Server 负责 SQLite 入库、SHA-256 哈希计算和 LogRegistry 合约调用，Web 前端展示日志、审计和告警结果。审计阶段，系统重新计算日志得到 actualHash，并与数据库 expectedHash、链上 onChainHash 进行三方比对；哈希不一致时，生成 failed 审计记录和 hash_mismatch 告警。系统还引入合约代码存在性校验与历史合约地址回溯策略，减少本地链重启和多次部署造成的误判。实验结果表明，LogRegistry 合约 8 项测试全部通过；日志批量提交 100 次成功率为 100%，平均响应时间约 107.03 ms，吞吐量约 9.33 条/秒；100、500、1000 条数据批量审计 5 轮均成功，篡改检测实验能够识别日志内容被修改并生成告警。",

    "可信日志审计结果需要通过直观方式呈现给用户，才能支持日常查看、问题定位和答辩演示。系统后端虽然能够完成日志存储、链上写入和审计比对，但如果缺少前端展示，用户难以快速理解系统运行状态。因此，本系统需要提供 Web 可视化界面，集中展示日志、审计和告警相关信息。从功能需求看，前端需要提供系统总览页面，用于展示日志数量、审计通过情况、异常告警数量等统计指标；还需要提供告警管理页面，用于展示 hash_mismatch 告警及其处理状态。可视化展示需求不仅服务于用户交互，也有助于验证系统闭环是否完整。":
        "可信日志审计结果需要以清晰方式呈现，才能支撑日常查看、问题定位和系统演示。后端完成日志存储、链上写入和审计比对后，仍需通过 Web 前端将日志规模、审计状态、异常告警和趋势变化转化为可理解的界面信息。从功能需求看，前端应提供系统总览、日志中心、审计管理和告警管理等页面，展示日志数量、审计通过情况、failed 与 pending 状态、hash_mismatch 告警及其处理状态。可视化展示不仅服务于用户交互，也用于说明采集、存证、审计和告警闭环是否真正贯通。",

    "系统可用性要求用户能够稳定地完成日志提交、审计执行、告警查看和结果展示等操作。对于本系统而言，可用性不仅体现在前端页面能否访问，还体现在日志采集、后端接口、数据库读写和链上交互能否形成稳定闭环。若任一环节不可用，系统就难以完成可信日志审计目标。在日志采集侧，Agent 需要支持增量读取、失败重试和状态同步，以降低临时异常对日志提交的影响。在后端服务侧，系统需要提供日志提交、审计执行、告警查询和总览统计等接口，使前端能够稳定获取数据。此外，系统可用性还体现在验证和演示过程的可重复性上。":
        "系统可用性要求日志提交、审计执行、告警查看和结果展示等操作能够稳定完成。对本系统而言，可用性并不只表现为前端页面可访问，还取决于 Agent 采集、后端接口、SQLite 读写和 LogRegistry 交互能否连续运行。日志采集侧通过增量读取、失败重试和状态同步降低临时异常对提交链路的影响；后端服务侧通过日志提交、审计执行、告警查询和总览统计等接口向前端提供稳定数据；链上交互侧通过合约地址记录和代码存在性校验减少实验环境变化造成的误判。上述设计共同保证原型系统在验证和演示过程中的可重复性。",

    "可维护性要求系统结构清晰、模块职责明确，便于后续修改、调试和论文说明。本系统采用模块化 Monorepo 结构，将前端 Web、后端 Server、采集 Agent、智能合约和共享类型库放在同一仓库下统一管理。不同模块在目录和职责上独立，能够降低代码耦合度，也便于在论文中说明系统分层设计。后端服务采用 routes、controllers、services、repositories、db 和 blockchain 等分层结构。":
        "可维护性要求系统结构清晰、模块职责明确，便于后续修改、调试和论文说明。本系统采用模块化 Monorepo 结构，将前端 Web、后端 Server、采集 Agent、智能合约和共享类型库放在同一仓库下统一管理。不同模块在目录和职责上相对独立，能够降低代码耦合度，也便于说明系统分层设计。后端服务进一步划分 routes、controllers、services、repositories、db 和 blockchain 等层次，使接口处理、业务编排、数据库访问和链上交互保持清晰边界。",

    "可扩展性方面，当前系统虽然基于 SQLite 和本地 Hardhat 链实现，但整体结构为后续扩展预留了空间。":
        "在可扩展性方面，当前系统虽然基于 SQLite 和本地 Hardhat 链实现，但整体结构为后续替换数据库、接入联盟链或测试网、扩展用户角色和引入异步审计队列预留了空间。",

    "本系统采用分层和模块化架构，由 Agent、Server、SQLite、LogRegistry 和 Web 五类核心部分组成，分别承担日志采集、业务处理、链下存储、链上存证和前端展示职责。Agent 位于数据流入口，负责从本地日志文件读取新增内容，并封装任务标识、来源路径、日志级别和采集时间等字段后提交 Server。":
        "本系统采用分层和模块化架构，由 Agent、Server、SQLite、LogRegistry 和 Web 五类核心部分组成，分别承担日志采集、业务处理、链下存储、链上存证和前端展示职责。Agent 位于数据流入口，负责从本地日志文件读取新增内容，并将任务标识、来源路径、日志级别和采集时间等字段封装后提交至 Server，为后续哈希计算和审计回溯提供统一输入。",

    "Server 是系统业务处理中心，负责接收日志、写入 SQLite、计算 SHA-256 哈希、调用 LogRegistry 合约以及执行审计任务。SQLite 保存 logs、log_hash_records、audit_records、alerts 和 agent_states 等数据，为查询和展示提供基础。LogRegistry 仅保存日志哈希和必要元数据，通过 LOGGER_ROLE 控制写入权限。":
        "Server 是系统业务处理中心，负责接收日志、写入 SQLite、计算 SHA-256 哈希、调用 LogRegistry 合约以及执行审计任务。SQLite 保存 logs、log_hash_records、audit_records、alerts 和 agent_states 等数据，为查询、展示和审计重算提供链下数据基础。LogRegistry 仅保存日志哈希和必要元数据，并通过 LOGGER_ROLE 控制写入权限，使链上记录成为独立于数据库的校验依据。",

    "Web 前端面向用户展示系统总览、日志列表、审计结果和告警信息。从数据流看，日志先由 Agent 提交至 Server，再写入 SQLite 并完成链上存证；审计时，Server 同时读取链下原文、数据库哈希和链上哈希，完成三方比对并将结果展示到前端。":
        "Web 前端面向用户展示系统总览、日志列表、审计结果和告警信息。从数据流看，日志先由 Agent 提交至 Server，再写入 SQLite 并完成链上存证；审计时，Server 同时读取链下原文、数据库哈希和链上哈希，完成三方比对并将结果返回前端。系统总体架构如图 4-1 所示，各部分通过明确的数据边界共同支撑可信日志审计流程。",

    "本系统按照日志可信审计的业务流程划分功能模块，主要包括日志采集 Agent 模块、后端服务模块、区块链存证模块、审计告警模块和前端可视化模块。":
        "本系统按照日志可信审计的业务流程划分功能模块，主要包括日志采集 Agent 模块、后端服务模块、区块链存证模块、审计告警模块和前端可视化模块。各模块围绕同一条数据链路协作，既保持职责边界，又保证日志能够从采集入口进入存证、审计和展示环节。",

    "日志采集 Agent 模块负责从本地日志文件中获取新增日志内容，是系统数据进入可信审计流程的入口。该模块的输入主要是指定路径下的日志文件和本地保存的读取偏移量，输出则是封装后的日志提交数据和 Agent 运行状态。":
        "日志采集 Agent 模块负责从本地日志文件中获取新增日志内容，是系统数据进入可信审计流程的入口。该模块的输入主要是指定路径下的日志文件和本地保存的读取偏移量，输出为封装后的日志提交数据和 Agent 运行状态。Agent 与后端服务之间通过接口交互，提交成功后再更新偏移量，从而降低重复采集和遗漏采集的风险。",

    "区块链存证模块负责将日志哈希写入链上，并在审计阶段提供链上记录查询能力。该模块的核心是 LogRegistry 智能合约，其输入主要包括任务标识 taskId 和日志哈希 logHash，输出则是链上存证记录、交易哈希、区块号和查询结果。":
        "区块链存证模块负责将日志哈希写入链上，并在审计阶段提供链上记录查询能力。该模块的核心是 LogRegistry 智能合约，其输入包括任务标识 taskId 和日志哈希 logHash，输出为链上存证记录、交易哈希、区块号和查询结果。模块通过 LOGGER_ROLE 限定可信写入主体，并通过 getLogsByTaskId 等方法支持任务维度审计。",

    "前端可视化模块负责将系统运行状态、日志数据、审计结果和告警信息展示给用户。该模块的输入主要来自后端 API 返回的数据，输出则是面向用户的页面视图。":
        "前端可视化模块负责展示系统运行状态、日志数据、审计结果和告警信息。该模块的输入来自后端 API 返回的数据，输出为面向用户的页面视图。前端不直接访问 SQLite 或 LogRegistry，而是通过后端接口获取日志、审计、告警和统计结果，从而保持前后端职责分离。",

    "本章围绕系统需求分析与总体设计展开，首先从功能需求角度说明了日志自动采集、链上可信存证、审计校验、异常告警和可视化展示等能力。随后，本章分析了系统的非功能需求，包括数据可信性、系统可用性以及可维护性与可扩展性。系统可用性要求 Agent、Server、SQLite、LogRegistry 和 Web 能够稳定协作；在总体设计方面，本章给出了系统总体架构、功能模块划分、数据流与业务流程以及数据库设计。":
        "本章围绕系统需求分析与总体设计展开，首先从功能需求角度说明日志自动采集、链上可信存证、审计校验、异常告警和可视化展示等能力，随后分析数据可信性、系统可用性以及可维护性与可扩展性等非功能需求。在总体设计方面，本章给出了 Agent、Server、SQLite、LogRegistry 和 Web 的协作关系，并进一步说明功能模块划分、系统数据流、业务流程和数据库表结构。",

    "系统由日志采集 Agent、后端服务、SQLite 链下存储、LogRegistry 链上存证、审计告警和前端可视化等部分组成。":
        "上述设计为第 5 章的系统实现、功能测试和实验验证提供了结构基础，也使第 3 章提出的可信日志审计机制具备可落地的工程载体。",

    "services 层是后端业务逻辑的主要承载位置。日志服务负责日志查询和 Agent 状态同步，区块链服务负责组织日志入库、SHA-256 哈希计算和 LogRegistry 写入，审计执行服务负责批量审计、单条审计和三方哈希比对，告警服务负责异常告警查询，总览服务负责聚合日志数量、审计数量和告警数量等指标。核心接口围绕系统主流程展开。GET /api/logs 用于日志中心列表展示；":
        "services 层是后端业务逻辑的主要承载位置。日志服务负责日志查询和 Agent 状态同步，区块链服务负责组织日志入库、SHA-256 哈希计算和 LogRegistry 写入，审计执行服务负责批量审计、单条审计和三方哈希比对，告警服务负责异常告警查询，总览服务负责聚合日志数量、审计数量和告警数量等指标。核心接口围绕系统主流程展开：POST /api/logs 用于接收 Agent 或测试脚本提交的日志；GET /api/logs 用于日志中心列表展示；POST /api/audits/run 用于触发审计任务；GET /api/alerts 用于告警管理页面查询异常记录。通过上述接口，后端将采集、存证、审计和展示连接为连续服务。",

    "智能合约功能测试主要面向 LogRegistry 合约展开，测试目标是确认合约能够稳定完成日志哈希存证、权限控制和查询操作。LogRegistry 合约负责保存 taskId、logHash、createdAt 和 submitter 四类核心字段，是系统链上可信存证的基础。测试内容覆盖合约部署、LOGGER_ROLE 权限控制、日志哈希写入、按记录编号查询、按 taskId 查询、任务日志数量统计、合约总记录数量统计以及异常输入处理等场景。从系统闭环角度看，合约测试通过说明链上存证模块能够为后端提供稳定接口。":
        "智能合约功能测试主要面向 LogRegistry 合约展开，测试目标是确认合约能够稳定完成日志哈希存证、权限控制和查询操作。LogRegistry 合约负责保存 taskId、logHash、createdAt 和 submitter 四类核心字段，是系统链上可信存证的基础。测试内容覆盖合约部署、LOGGER_ROLE 权限控制、日志哈希写入、按记录编号查询、按 taskId 查询、任务日志数量统计、合约总记录数量统计以及异常输入处理等场景。测试结果显示，LogRegistry 合约 8 项测试全部通过，说明链上存证模块能够为后端提供稳定接口。",

    "后端接口与审计闭环测试主要验证 Server 能否正确完成日志接收、链下入库、哈希计算、链上写入、审计比对和异常告警。通过这些接口，可以验证后端是否能够将第 3 章设计的可信日志审计机制落到实际运行流程中。在链上写入验证中，系统通过后端服务接收日志内容，将日志原文写入 logs 表，并对 log_content 计算 SHA-256 哈希。在 API 回归验证中，系统对日志提交、日志查询、审计触发、审计结果查询和告警查询等接口进行检查，确认接口能够返回结构化响应，并且前后端使用的数据字段保持一致。":
        "后端接口与审计闭环测试主要验证 Server 能否正确完成日志接收、链下入库、哈希计算、链上写入、审计比对和异常告警。通过这些接口，可以验证第 3 章设计的可信日志审计机制是否能够落到实际运行流程中。在链上写入验证中，系统通过后端服务接收日志内容，将日志原文写入 logs 表，并对 log_content 计算 SHA-256 哈希，再调用 LogRegistry 完成存证。在 API 回归验证中，系统对日志提交、日志查询、审计触发、审计结果查询和告警查询等接口进行检查，确认接口能够返回结构化响应，并且前后端使用的数据字段保持一致。同时，测试覆盖 passed 与 failed 分支，能够验证审计通过和哈希不一致告警两类结果。",

    "该测试说明，后端不只是完成日志保存，还能够将链下数据库、链上合约和审计告警串联起来。":
        "该测试说明，后端不只是完成日志保存，还能够将链下数据库、链上合约和审计告警串联起来，使可信日志审计闭环具备实际运行基础。",

    "前端展示测试主要通过 TypeScript 类型检查与 Vite 构建验证完成。总体来看，Agent 采集、后端审计和前端展示能够形成连续路径：Agent 负责产生标准化日志输入，后端负责可信存证与审计判断，前端负责展示运行状态和异常结果。":
        "前端展示测试主要通过 TypeScript 类型检查与 Vite 构建验证完成，验证结果表明前端类型约束和构建流程能够通过。总体来看，Agent 采集、后端审计和前端展示形成了连续路径：Agent 负责产生标准化日志输入，后端负责可信存证与审计判断，前端负责展示运行状态和异常结果。",

    "本章围绕系统实现与实验验证展开，首先说明了系统开发环境和 Monorepo 工程结构，随后分别介绍了日志采集 Agent、后端服务与数据持久化、前端可视化模块的实现方式。在测试与实验层面，本章完成了智能合约功能测试、后端接口与审计闭环测试、Agent 采集与前端展示测试。":
        "本章围绕系统实现与实验验证展开，首先说明系统开发环境和 Monorepo 工程结构，随后分别介绍日志采集 Agent、后端服务与数据持久化、前端可视化模块的实现方式。在测试与实验层面，本章完成智能合约功能测试、后端接口与审计闭环测试、Agent 采集与前端展示测试，并进一步开展日志批量提交、批量审计性能和篡改检测实验。",

    "测试结果表明，LogRegistry 合约 8 项测试全部通过，后端能够验证链上写入、API 回归以及 passed/failed 审计分支，Agent 能够完成增量读取、偏移量持久化和真实提交，前端 TypeScript 检查与 Vite 构建通过。":
        "测试结果表明，LogRegistry 合约 8 项测试全部通过；后端能够完成链上写入验证、API 回归以及 passed/failed 审计分支验证；Agent 能够完成增量读取、偏移量持久化和真实提交；前端 TypeScript 检查与 Vite 构建通过。实验结果进一步说明，系统在原型环境下能够运行采集、存证、审计、告警和展示闭环，并能够识别日志内容被修改的情况。",

    "本文围绕可信任务日志审计系统完成了从机制设计到工程实现的工作。针对中心化日志易被篡改、删除或伪造的问题，系统采用链下保存日志原文、链上保存哈希摘要的方式，将日志内容与区块链存证结合起来，形成可核验的审计依据。":
        "本文围绕可信任务日志审计系统完成了从机制设计到工程实现的研究工作。针对中心化日志易被篡改、删除或伪造，且审计结果缺少独立可信依据的问题，系统采用链下保存日志原文、链上保存哈希摘要的方式，将日志内容与区块链存证结合起来。该模型既避免日志原文直接上链带来的成本和隐私问题，又使后续审计能够借助链上哈希获得独立校验依据。",

    "在系统实现方面，本文设计了 Agent 增量采集、Express 后端服务、SQLite 链下存储、LogRegistry 智能合约、审计告警和 Web 可视化模块。审计阶段通过 expectedHash、actualHash 与 onChainHash 三方比对判断日志状态，并在 failed 时生成 hash_mismatch 告警。":
        "在系统设计与实现方面，本文构建了 Agent 增量采集、Express 后端服务、SQLite 链下存储、LogRegistry 智能合约、审计告警和 Web 可视化模块。Agent 负责读取日志文件新增内容并维护采集偏移量；Server 负责日志接收、入库、哈希计算、合约调用和审计执行；SQLite 保存 logs、log_hash_records、audit_records、alerts 和 agent_states 等链下数据；LogRegistry 保存 taskId、logHash、createdAt 和 submitter，并通过 LOGGER_ROLE 控制日志哈希写入权限；Web 前端用于展示系统总览、日志中心、审计结果和告警信息。",

    "在验证方面，系统完成了合约测试、接口回归、Agent 采集、前端构建、批量提交、批量审计和篡改检测实验。实验结果表明，系统能够运行完整闭环，并能识别日志内容被修改的情况。":
        "在审计机制方面，本文设计 expectedHash、actualHash 与 onChainHash 三方哈希比对规则。expectedHash 来源于 log_hash_records，actualHash 来源于审计阶段对日志原文的重新计算，onChainHash 来源于 LogRegistry 查询。三者一致时审计结果为 passed，不一致时判定为 failed，链上记录缺失或合约不可用时记录为 pending。系统还针对 Hardhat 本地链重启和多次部署问题，引入 log_hash_records.contract_address 历史合约地址回溯和 provider.getCode(address) 合约代码存在性校验，增强了实验环境下审计结果的可解释性。在验证方面，系统完成合约测试、接口回归、Agent 采集、前端构建、批量提交、批量审计和篡改检测实验，结果表明系统能够运行完整闭环，并能识别日志内容被修改的情况。",

    "本文的创新点主要体现在工程机制组合与可信审计闭环设计上。第一，系统采用链下日志原文与链上哈希摘要结合的轻量存证模型，在避免日志原文直接上链的同时，保留链上可验证证据，兼顾隐私、成本和审计需求。":
        "本文的创新点主要体现在工程机制组合与可信审计闭环设计上，并不将其夸大为区块链或密码学理论突破。第一，系统采用链下日志原文与链上哈希摘要结合的轻量存证模型，在避免日志原文直接上链的同时，保留链上可验证证据，兼顾隐私、成本和审计需求。该模型适合日志数量多、内容长度不固定且需要后续查询展示的任务日志场景。",

    "第二，系统设计 expectedHash、actualHash 和 onChainHash 三方哈希比对机制，将数据库记录、当前日志内容和链上存证同时纳入审计判断，增强了日志完整性校验的可信依据。第三，系统在工程实践中加入历史合约地址回溯和 provider.getCode(address) 合约代码存在性校验，减少本地链重启和多次部署导致的误判。":
        "第二，系统设计 expectedHash、actualHash 和 onChainHash 三方哈希比对机制，将数据库记录、当前日志内容和链上存证同时纳入审计判断。相比只比较链下数据库哈希和当前日志内容，三方比对能够进一步说明链上证据与链下记录是否一致。第三，系统在工程实践中加入历史合约地址回溯和 provider.getCode(address) 合约代码存在性校验，减少本地链重启和多次部署导致的误判，使实验过程中的链上读取依据更加清晰。",

    "第四，系统实现了采集、存证、审计、告警和展示闭环。该创新不属于理论突破，而是在毕业设计原型中将区块链存证、日志采集和异常告警整合为可运行系统。":
        "第四，系统实现了采集、存证、审计、告警和展示闭环。Agent 将新增日志持续送入系统，Server 完成链下保存和链上存证，审计模块执行三方比对，告警模块在 failed 时生成 hash_mismatch 记录，Web 前端展示审计和告警结果。该创新属于面向毕业设计原型的工程整合，重点在于将可信存证、完整性校验和异常提示组织为可运行流程。",

    "当前系统仍存在一定局限。首先，实验环境主要基于本地 Hardhat 链，尚未接入公开测试网或联盟链，因此链上共识环境、节点稳定性和实际部署成本仍未充分验证。其次，SQLite 更适合原型系统和小规模实验，在高并发日志写入和大规模数据分析场景下能力有限。":
        "当前系统仍存在一定局限。首先，实验环境主要基于本地 Hardhat 链，尚未接入公开测试网或联盟链，因此链上共识环境、节点稳定性、交易确认延迟和实际部署成本仍未充分验证。其次，SQLite 更适合原型系统和小规模实验，在高并发日志写入、大规模数据分析和多用户协同访问场景下能力有限，后续若面向真实业务部署，需要更换为更适合生产环境的数据库方案。",

    "此外，批量审计流程以顺序处理为主，随着数据规模增加，审计耗时明显上升，后续仍需引入异步队列、批处理或并发审计优化。权限体系目前主要围绕 LOGGER_ROLE 展开，对多角色用户、审计人员权限和告警处置流程支持不足。系统也尚未深入分析日志语义，异常判断主要依赖哈希一致性。":
        "此外，批量审计流程以顺序处理为主，随着数据规模增加，审计耗时明显上升，后续仍需引入异步队列、批处理或并发审计优化。权限体系目前主要围绕 LOGGER_ROLE 展开，对多角色用户、审计人员权限和告警处置流程支持不足。系统也尚未深入分析日志语义，异常判断主要依赖哈希一致性，能够识别内容是否被修改，但不能进一步判断日志语义风险或业务异常类型。",

    "后续可从部署环境、性能和安全治理三个方面优化系统。首先，可将 LogRegistry 部署到联盟链或公开测试网，验证多节点环境下的存证稳定性和交易确认情况。其次，可引入更完整的多角色权限体系，区分日志提交者、审计人员、管理员和告警处理人员，使系统更接近实际业务场景。":
        "后续可从部署环境、性能和安全治理三个方面优化系统。首先，可将 LogRegistry 部署到联盟链或公开测试网，验证多节点环境下的存证稳定性、交易确认情况和合约调用成本。其次，可引入更完整的多角色权限体系，区分日志提交者、审计人员、管理员和告警处理人员，使日志写入、审计执行、告警确认和系统管理形成更明确的权限边界。",

    "在性能方面，可设计异步审计队列、批量哈希计算和并发链上查询机制，降低大规模审计耗时。数据存储层也可从 SQLite 扩展到更适合生产环境的数据库。在功能方面，可结合日志语义分析和风险分级，对错误日志、异常任务和高风险操作进行更细粒度识别，并通过前端提供更完善的统计图表和告警追踪能力。":
        "在性能方面，可设计异步审计队列、批量哈希计算和并发链上查询机制，降低大规模审计耗时。数据存储层也可从 SQLite 扩展到更适合生产环境的数据库，并结合索引优化提升查询效率。在功能方面，可结合日志语义分析和风险分级，对错误日志、异常任务和高风险操作进行更细粒度识别，并通过前端提供更完善的统计图表、告警追踪和审计历史分析能力。",

    "本章总结了系统研究工作、主要创新点、存在不足和后续优化方向。总体来看，本文实现了基于区块链的可信任务日志审计原型系统，验证了链上链下混合存储、三方哈希比对和异常告警机制的可行性。同时，系统仍需在真实链环境、性能优化、权限体系和日志语义分析方面继续完善。":
        "本章对全文研究工作、主要创新点、存在不足和后续优化方向进行了总结。总体来看，本文实现了基于区块链的可信任务日志审计原型系统，验证了链上链下混合存储、三方哈希比对、历史合约地址回溯、合约代码存在性校验和异常告警机制的可行性。与此同时，系统仍需在真实链环境、性能优化、权限体系和日志语义分析方面继续完善。",

    "在毕业设计和论文撰写即将完成之际，谨向在本课题研究、系统实现和论文写作过程中给予帮助与支持的老师、同学和家人表示诚挚感谢。首先，感谢指导教师在选题确定、系统设计、论文结构调整和写作规范方面给予的耐心指导。相关意见使论文能够更加突出链上链下混合存储、智能合约存证、三方哈希比对和异常告警等核心内容，也使毕业设计的整体思路更加清晰。其次，感谢学院各位老师在本科阶段课程学习和实践训练中给予的帮助。":
        "在毕业设计和论文撰写即将完成之际，谨向在本课题研究、系统实现和论文写作过程中给予帮助与支持的老师、同学和家人表示诚挚感谢。首先，感谢指导教师在选题确定、系统设计、论文结构调整和写作规范方面给予的耐心指导。相关意见使论文能够更加突出链上链下混合存储、智能合约存证、三方哈希比对和异常告警等核心内容，也使毕业设计的整体思路更加清晰。其次，感谢学院各位老师在本科阶段课程学习和实践训练中给予的帮助，使本课题能够在软件工程方法、系统设计思路和工程实现规范的基础上逐步推进。同时，感谢同学在系统调试、资料整理和论文修改过程中的交流与帮助，也感谢家人在毕业设计期间给予的理解和支持。毕业设计是本科阶段专业学习和实践能力的一次综合检验，后续仍将保持严谨踏实的学习态度，在软件工程实践中继续提升分析问题、设计系统和解决问题的能力。"
}


CAPTION_REPLACEMENTS = {
    "表 3-2 LogRegistry 合约方法说明": "表 3-1 LogRegistry 合约方法说明",
}


def full_text(doc: Document) -> str:
    return "\n".join(p.text.strip() for p in doc.paragraphs if p.text.strip())


def insert_paragraph_before_table(table: Table, text: str) -> None:
    new_p = OxmlElement("w:p")
    table._tbl.addprevious(new_p)
    paragraph = Paragraph(new_p, table._parent)
    paragraph.text = text


def add_table_after_paragraph(doc: Document, paragraph: Paragraph, rows: list[list[str]]) -> None:
    table = doc.add_table(rows=len(rows), cols=len(rows[0]))
    table.style = "Table Grid"
    for row_index, row in enumerate(rows):
        for col_index, value in enumerate(row):
            table.cell(row_index, col_index).text = value
    tbl = table._tbl
    doc.element.body.remove(tbl)
    paragraph._p.addnext(tbl)


def ensure_table_captions(doc: Document) -> int:
    inserted = 0
    text = full_text(doc)
    tables = list(doc.tables)

    if len(tables) >= 2 and "表 3-2 审计状态判定规则" not in text:
        insert_paragraph_before_table(tables[1], "表 3-2 审计状态判定规则")
        inserted += 1

    text = full_text(doc)
    tables = list(doc.tables)
    if len(tables) >= 4 and "表 5-1 工程目录说明" not in text:
        insert_paragraph_before_table(tables[3], "表 5-1 工程目录说明")
        inserted += 1

    text = full_text(doc)
    if "表 5-2 性能实验与篡改检测结果" in text and "实验项目\t验证结果\t说明" not in text:
        for paragraph in doc.paragraphs:
            if paragraph.text.strip() == "表 5-2 性能实验与篡改检测结果":
                add_table_after_paragraph(
                    doc,
                    paragraph,
                    [
                        ["实验项目", "验证结果", "说明"],
                        ["智能合约功能测试", "8 项测试全部通过", "验证 LogRegistry 写入、查询和权限控制"],
                        ["日志批量提交实验", "100 次成功率为 100%，平均响应时间约 107.03 ms，吞吐量约 9.33 条/秒", "验证连续提交与存证链路"],
                        ["批量审计性能实验", "100/500/1000 条数据 5 轮全部成功，平均耗时约 3067.77/15659.44/35032.13 ms", "验证批量审计闭环稳定性"],
                        ["篡改检测实验", "auditStatus 为 failed，alertGenerated 为 true", "验证哈希不一致识别与告警生成"],
                    ],
                )
                inserted += 1
                break

    return inserted


def main() -> None:
    if not DOCX_PATH.exists():
        raise FileNotFoundError(DOCX_PATH)

    backup = DOCX_PATH.with_name(
        f"{DOCX_PATH.stem}-before-full-polish-pass2-{datetime.now().strftime('%Y%m%d-%H%M%S')}{DOCX_PATH.suffix}"
    )
    shutil.copy2(DOCX_PATH, backup)

    doc = Document(DOCX_PATH)
    replaced = 0
    missing: list[str] = []

    for paragraph in doc.paragraphs:
        text = paragraph.text.strip()
        if text in CAPTION_REPLACEMENTS:
            paragraph.text = CAPTION_REPLACEMENTS[text]
            replaced += 1
        elif text in REPLACEMENTS:
            paragraph.text = REPLACEMENTS[text]
            replaced += 1

    current = full_text(doc)
    for old in [*CAPTION_REPLACEMENTS.keys(), *REPLACEMENTS.keys()]:
        if old in current:
            continue
        # It is normal for exact old strings to disappear after replacement; this
        # list is only for strings that were not present in the source document.
        if old not in full_text(Document(backup)):
            missing.append(old[:60])

    inserted_tables_or_captions = ensure_table_captions(doc)
    doc.save(DOCX_PATH)

    REPORT_PATH.write_text(
        "# 论文全文第二轮润色报告\n\n"
        f"- 目标文件：`{DOCX_PATH}`\n"
        f"- 备份文件：`{backup}`\n"
        f"- 精确替换段落数：{replaced}\n"
        f"- 补充表题或表格数：{inserted_tables_or_captions}\n"
        f"- 未命中的候选段落数：{len(missing)}\n\n"
        "## 本轮处理重点\n\n"
        "- 修正第 5.3.1 节接口说明中停在分号处的句子。\n"
        "- 统一第 3 章表格编号，补充审计状态规则表题。\n"
        "- 补充第 5.1 工程目录说明表题和第 5.6 实验结果表格。\n"
        "- 强化第 4、5、6 章的工程叙述、图文衔接和章节收束。\n"
        "- 保留 LogRegistry、LOGGER_ROLE、三方哈希比对、历史合约地址回溯、合约代码存在性校验和真实实验数据。\n",
        encoding="utf-8",
    )
    print(f"backup={backup}")
    print(f"replaced={replaced}")
    print(f"inserted_tables_or_captions={inserted_tables_or_captions}")
    print(f"missing_candidates={len(missing)}")


if __name__ == "__main__":
    main()
