{
  "name": "graduation-project",
  "version": "1.0.0",
  "description": "This project is the graduation project of Charlie Dai.",
  "main": "index.js",
  "directories": {
    "doc": "doc",
    "test": "tests"
  },
  "scripts": {
    "dev": "npx concurrently \"npm --prefix packages/contracts run node\" \"npm --prefix apps/server run dev\" \"npm --prefix apps/agent run dev\" \"npm --prefix apps/web run dev\"",
    "chain:deploy": "npm --prefix packages/contracts run deploy:local",
    "verify:contracts": "npm --prefix packages/contracts test",
    "verify:server": "node scripts/verify-server.js",
    "verify:agent": "node scripts/verify-agent.js",
    "verify:web": "npm --prefix apps/web run build",
    "verify:all": "npm run verify:contracts && npm run verify:server && npm run verify:agent && npm run verify:web",
    "bench:logs": "node scripts/run-benchmark.js logs",
    "bench:audits": "node scripts/run-benchmark.js audits",
    "test": "npm run verify:all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
